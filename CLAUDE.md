# CLAUDE.md

> **This file is a pointer.** The canonical contributor guide is
> [`AGENTS.md`](./AGENTS.md) — read it first and follow it. Do not duplicate
> its content here.
>
> Claude-specific notes: this repo is developed primarily with AI agents, so
> favor small sliced commits, run the full gate (`tsc`, `lint`, `format:check`,
> `test`, `build`) before claiming anything is done, and never mix a Prettier
> reformat with functional changes.
